
# #######################################################################################
# КОНФИГ HYPRLAND (изначально автосгенерирован, дальше правится руками).
# #######################################################################################

# Не все доступные опции выставлены здесь — полный список см. в вики:
# https://wiki.hypr.land/Configuring/
#
# Конфиг можно разбивать на несколько файлов и подключать через:
# source = ~/.config/hypr/myColors.conf

###################
### ПРОГРАММЫ   ###
###################

# См. https://wiki.hypr.land/Configuring/Keywords/
$terminal = kitty
$fileManager = thunar
$menu = rofi -show drun -show-icons -calc-command "echo -n '{result}' | wl-copy"
$tabsNum = 4
$prevTab = $(( ( $(hyprctl activeworkspace -j | jq '.id') + $tabsNum - 2 ) % $tabsNum + 1 ))
$nextTab = $(( ( $(hyprctl activeworkspace -j | jq '.id') % $tabsNum ) + 1 ))
$getScreen = grim -g "$(slurp -b ffffff66 -w 1 && sleep 0.2)" 

#################
### АВТОЗАПУСК ###
#################
exec-once = dbus-update-activation-environment --systemd --all
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP XDG_SESSION_DESKTOP XDG_SESSION_TYPE BROWSER SSH_AUTH_SOCK SSH_ASKPASS
exec-once = systemctl --user start hyprpolkitagent.service 
exec-once = systemctl --user start swayosd.service 

exec-once = waybar 
exec-once = awww-daemon -q >/dev/null 2>&1 
exec-once = mako
exec-once = wl-paste --type text --watch cliphist store -max-items 500 store
exec-once = wl-paste --type image --watch cliphist store -max-items 1000 store
exec-once = hyprctl keyword cursor:no_hardware_cursors true && hyprctl keyword cursor:no_hardware_cursors false # Чинит битый программный курсор

exec = $HUIX/scripts/toggle-theme.sh --sync
exec = $HUIX/scripts/screen-shader.sh restore

#############################
### ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ   ###
#############################

# См. https://wiki.hypr.land/Configuring/Environment-variables/

env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = GTK_CSD,0
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland

cursor {
    no_hardware_cursors = false;
}


###################
### РАЗРЕШЕНИЯ  ###
###################

# См. https://wiki.hypr.land/Configuring/Permissions/
# Изменения разрешений требуют перезапуска Hyprland и не применяются на лету
# (из соображений безопасности).

# ecosystem {
#   enforce_permissions = 1
# }

permission = /usr/(bin|local/bin)/grim, screencopy, allow
permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### ВНЕШНИЙ ВИД    ###
#####################

# См. https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 10

    border_size = 2

    # Про цвета: https://wiki.hypr.land/Configuring/Variables/#variable-types
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # true — менять размер окон, таща за границы и гэпы
    resize_on_border = false

    # Перед включением прочитать https://wiki.hypr.land/Configuring/Tearing/
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 7
    rounding_power = 2

    # Прозрачность активного и неактивного окна
    active_opacity = 1.0
    inactive_opacity = 0.95

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Кривые по умолчанию, см. https://wiki.hypr.land/Configuring/Animations/#curves
    #        ИМЯ,            X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    # Анимации по умолчанию, см. https://wiki.hypr.land/Configuring/Animations/
    #           ИМЯ,           ВКЛ,   СКОР,  КРИВАЯ,       [СТИЛЬ]
    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     7,     quick
}

# См. https://wiki.hypr.land/Configuring/Workspace-Rules/
# «Умные гэпы» / «без гэпов, когда окно одно» — раскомментировать при желании.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule {
#     name = no-gaps-wtv1
#     match:float = false
#     match:workspace = w[tv1]
#
#     border_size = 0
#     rounding = 0
# }
#
# windowrule {
#     name = no-gaps-f1
#     match:float = false
#     match:workspace = f[1]
#
#     border_size = 0
#     rounding = 0
# }

# Подробнее: https://wiki.hypr.land/Configuring/Dwindle-Layout/
dwindle {
    preserve_split = true # обычно это то, что нужно
}

# Подробнее: https://wiki.hypr.land/Configuring/Master-Layout/
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 2 # 0 или 1 — отключить обои-маскота
    disable_hyprland_logo = false # true — убрать случайное лого Hyprland / фон с анимедевочкой :(
    # Если локер умер, не сняв лок (краш hyprlock), разрешаем новому инстансу
    # залочить сессию заново — иначе останется только tty
    allow_session_lock_restore = true
}


#############
### INPUT ###
#############

# Жесты тачпада, см. https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace
# Зум экрана щипком двумя пальцами (живая лупа вокруг курсора). Бинды на
# mouse_up/mouse_down (зум колесом ниже) работают ТОЛЬКО с физическим колесом
# мыши — на тачпаде скролл-бинды не срабатывают в принципе, поэтому для ноутбука
# зум повешен на встроенный жест cursorZoom (mode live — непрерывно по щипку).
gesture = 2, pinch, cursorZoom, 1, live

device {
  name = gaomon-gaomon-tablet_s630
  output = DP-1
}

xwayland {
  force_zero_scaling = true
}


###################
### БИНДЫ         ###
###################

$mainMod = SUPER
$mainScreenMod = SUPER ALT

binds {
    scroll_event_delay = 0
}

# Основные действия
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, W, exec, $menu
bind = $mainMod, F12, exec, loginctl lock-session

# Кнопка питания -> меню питания (logind HandlePowerKey=ignore, см. nixos/desktop/logind.nix)
bindl = , XF86PowerOff, exec, $HUIX/scripts/rofi-power.sh
bind = $mainMod, Delete, exec, $HUIX/scripts/rofi-power.sh
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, T, layoutmsg, togglesplit # в Hyprland 0.54+ togglesplit переехал под layoutmsg
bind = $mainMod, F, fullscreen, 0 # 1 — полноэкранный режим (сохраняет бары), 0 — полный фулскрин
bind = $mainMod, Z, exec, pidof waybar && pkill waybar || waybar
bind = $mainMod, Y, exec, rofi -show dictionary -modi "dictionary:$HUIX/scripts/rofi-wooordhunt.sh"
bind = $mainMod, U, exec, rofi -modi "ru-en:$HUIX/scripts/rofi-libre.sh ru en,en-ru:$HUIX/scripts/rofi-libre.sh en ru" -show ru-en 
bind = $mainMod, B, exec, $HUIX/scripts/rofi-clipboard.sh
bind = $mainMod, N, exec, $HUIX/scripts/rofi-notify.sh
bind = $mainMod SHIFT, N, exec, $HUIX/scripts/notify-center.sh dnd toggle
bind = $mainMod, A, exec, $HUIX/scripts/toggle-theme.sh
# SHIFT+G — пикер: каждый выбор ДОБАВЛЯЕТ эффект в стопку (композиция).
# G — сброс всей стопки (эффекты стакаются, пока не сбросить здесь или «Обычным»).
bind = $mainMod, G, exec, $HUIX/scripts/screen-shader.sh effect clear
bind = $mainMod SHIFT, G, exec, $HUIX/scripts/rofi-shader.sh
bindel = $mainMod CTRL, bracketright, exec, $HUIX/scripts/screen-shader.sh bright up
bindel = $mainMod CTRL, bracketleft, exec, $HUIX/scripts/screen-shader.sh bright down
bind = $mainMod CTRL, BackSpace, exec, $HUIX/scripts/screen-shader.sh bright reset
bind = $mainMod SHIFT, B, exec, rofi -show emoji -modi "emoji:rofimoji --action copy --files emojis_smileys_emotion emojis_people_body emojis_animals_nature emojis_food_drink emojis_activities emojis_travel_places emojis_objects emojis_symbols emojis_flags dingbats miscellaneous_symbols_and_pictographs,math:rofimoji --action copy --files math mathematical_operators arrows geometric_shapes greek_and_coptic,chars:rofimoji --action copy --files currency_symbols letterlike_symbols latin-1_supplement general_punctuation superscripts_and_subscripts enclosed_alphanumerics box_drawing cjk_symbols_and_punctuation nerd_font,kaomoji:rofimoji --action copy --files kaomoji"

# Скриншоты / работа с экраном
bind = $mainScreenMod, S, exec, $getScreen - | GTK_THEME=Adwaita:light satty -f -
bind = $mainScreenMod, C, exec, $HUIX/scripts/shot-copy.sh region
bind = $mainScreenMod, A, exec, $HUIX/scripts/pin-screen.sh
bind = $mainScreenMod, P, exec, $HUIX/scripts/colorpicker.sh
bind = $mainScreenMod, T, exec, $HUIX/scripts/ocr.sh rus+eng
bind = $mainScreenMod, R, exec, $HUIX/scripts/ocr.sh rus
bind = $mainScreenMod, E, exec, $HUIX/scripts/ocr.sh eng
bind = $mainScreenMod, M, exec, $HUIX/scripts/shot-copy.sh full

# Лупа / зум экрана колесом мыши (ALT+WIN+колесо) — только физическое колесо;
# на тачпаде вместо этого работает жест «щипок» (gesture cursorZoom выше)
bind = $mainScreenMod, mouse_down,   exec, $HUIX/scripts/zoom.sh in
bind = $mainScreenMod, mouse_up, exec, $HUIX/scripts/zoom.sh out
bind = $mainScreenMod, BackSpace, exec, $HUIX/scripts/zoom.sh reset

# --- НАВИГАЦИЯ (ФОКУС) ---
# Перемещение фокуса (HJKL)
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Перемещение фокуса (СТРЕЛКИ)
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# --- ПЕРЕМЕЩЕНИЕ ОКОН ---
# Перемещение самих окон (HJKL) + Shift
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

# Перемещение самих окон (СТРЕЛКИ) + Shift
bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, down, movewindow, d

# Точное перемещение активного окна (для плавающих)
binde = $mainMod CTRL, left, moveactive, -80 0
binde = $mainMod CTRL, right, moveactive, 80 0
binde = $mainMod CTRL, up, moveactive, 0 -80
binde = $mainMod CTRL, down, moveactive, 0 80

# То же самое для HJKL
binde = $mainMod CTRL, h, moveactive, -80 0
binde = $mainMod CTRL, l, moveactive, 80 0
binde = $mainMod CTRL, k, moveactive, 0 -80
binde = $mainMod CTRL, j, moveactive, 0 80

# --- РАБОЧИЕ СТОЛЫ ---
# Переключение воркспейсов [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
# bind = $mainMod, 0, workspace, 10

# Перенос окна на воркспейс [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Спец-воркспейс (скретчпад)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Мышь
bind = $mainMod, mouse_down, exec, hyprctl dispatch workspace $nextTab
bind = $mainMod, mouse_up,exec, hyprctl dispatch workspace $prevTab
bind = $mainMod SHIFT, mouse_down, exec, hyprctl dispatch movetoworkspace $nextTab
bind = $mainMod SHIFT, mouse_up, exec, hyprctl dispatch movetoworkspace $prevTab

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Медиа клавиши и яркость
bindel = , XF86AudioRaiseVolume, exec, swayosd-client --output-volume raise
bindel = , XF86AudioLowerVolume, exec, swayosd-client --output-volume lower
bindl  = , XF86AudioMute,        exec, swayosd-client --output-volume mute-toggle
bindl  = , XF86AudioMicMute,     exec, swayosd-client --input-volume mute-toggle
bindel = , XF86MonBrightnessUp,   exec, swayosd-client --brightness raise
bindel = , XF86MonBrightnessDown, exec, swayosd-client --brightness lower

# Управление плеером
bindl = , XF86AudioNext,  exec, playerctl -p playerctld next
bindl = , XF86AudioPrev,  exec, playerctl -p playerctld previous
bindl = , XF86AudioPlay,  exec, playerctl -p playerctld play-pause
bindl = , XF86AudioPause, exec, playerctl -p playerctld play-pause

# Изменение размера активного окна
binde = SUPER ALT, L, resizeactive, 40 0
binde = SUPER ALT, H, resizeactive, -40 0
binde = SUPER ALT, K, resizeactive, 0 -40
binde = SUPER ALT, J, resizeactive, 0 40

binde = SUPER ALT, right, resizeactive, 40 0
binde = SUPER ALT, left, resizeactive, -40 0
binde = SUPER ALT, up, resizeactive, 0 -40
binde = SUPER ALT, down, resizeactive, 0 40

# Правила окон: https://wiki.hypr.land/Configuring/Window-Rules/
# Правила воркспейсов: https://wiki.hypr.land/Configuring/Workspace-Rules/

# Полезные правила окон

windowrule {
    # Игнорировать запросы на максимизацию от всех приложений — обычно так удобнее.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Фикс проблем с перетаскиванием под XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Правило окна для hyprland-run
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

# layerrule {
#     name = no_anim_for_selection
#     no_anim = on
#     match:namespace = selection
# }

layerrule {
    name = no_anim_for_hyprpicker
    no_anim = on
    match:namespace = ^(hyprpicker)$
}

# Фикс для невидимых окон перетаскивания (у них обычно пустой title)
windowrule {
    name = ue4-drag-empty-fix
    match:class = ^(UE4Editor|UnrealEditor)$
    match:title = ^$
    
    no_initial_focus = true
    no_focus = true
}

windowrule {
    name = float-dialect
    match:class = ^(app.drey.Dialect)$
    float = yes
}

# pin-screen.sh: swayimg добавляет _<pid> к классу, поэтому матчим по префиксу.
# Плавающее + поверх окон, точную геометрию задаёт сам swayimg (--position/--size).
windowrule {
    name = float-desktop-pin
    match:class = ^(desktop-pin).*$
    float = yes
    pin = yes
}


layerrule = blur on, match:namespace waybar
layerrule = animation slide, match:namespace waybar
